---
title: BMatesProvider
description: 라이브러리 테마와 공통 스타일 컨텍스트를 제공하는 Provider입니다.
navTitle: BMatesProvider
---

# BMatesProvider

라이브러리 테마와 공통 스타일 컨텍스트를 제공하는 Provider입니다.

## 가져오기

```tsx
import { BMatesProvider } from 'bmates-ui';
```

## 기본 사용법

아래 예제는 기본 Storybook 구성을 바탕으로 하며 구현을 시작할 때 활용할 수 있습니다.

```tsx
<BMatesProvider>
  <Button>Ready</Button>
</BMatesProvider>
```

## 미리보기

:::: playground[BMatesProvider]{isolation="inline"}
<ClientPreview name="bmates-preview" props={{ component: "provider", example: "default" }} minHeight={132} />
```tsx
<BMatesProvider>
  <Button>Ready</Button>
</BMatesProvider>
```
::::

## 주요 속성 미리보기

아래 예제에서는 시각적 결과나 상호작용을 바꾸는 주요 속성을 각각 확인할 수 있습니다.

### Provider 콘텐츠

:::: playground[Provider 콘텐츠]{isolation="inline"}
<ClientPreview name="bmates-preview" props={{ component: "provider", example: "default" }} minHeight={132} />
```tsx
<BMatesProvider><Button>Child</Button></BMatesProvider>
```
::::

## 컴포넌트 API

:::: api-list
::: api-item
:property: `children`
:type: `ReactNode`
BMates 컨텍스트를 전달받는 애플리케이션 하위 트리입니다.
:::
::::

## 사용 참고사항

- 문서에 표시된 모든 컴포넌트는 `bmates-ui`에서 내보냅니다.
- 별도 설명이 없다면 각 컴포넌트는 기반 HTML 요소의 표준 속성도 지원합니다.
- 애플리케이션이 상태를 관리한다면 `default*` 속성만 사용하지 말고 제어 값과 변경 핸들러를 함께 사용합니다.
