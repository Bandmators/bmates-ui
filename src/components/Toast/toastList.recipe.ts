import { createRecipe } from '@/styles/recipe';

export const toastListRecipe = createRecipe('bm-toast-list', {
  position: ['top-left', 'top-center', 'top-right', 'bottom-left', 'bottom-center', 'bottom-right'],
});
