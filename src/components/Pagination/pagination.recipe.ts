import { cva } from '../../../styled-system/css';

export const paginationListRecipe = cva({
  base: { listStyle: 'none', margin: '0px', padding: '0px', display: 'flex', flexDirection: 'row', gap: '0.5rem' },
});
