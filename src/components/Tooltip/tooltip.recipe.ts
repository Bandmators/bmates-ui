import { createRecipe } from '@/styles/recipe';

export const tooltipTriggerRecipe = createRecipe('bm-tooltip', { direction: ['top', 'bottom', 'left', 'right'] });
export const tooltipWrapperRecipe = createRecipe('bm-tooltip__wrapper');
